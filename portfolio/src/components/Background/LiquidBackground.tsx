import React from 'react';

// Pure subtle grid background — no blob gradients
const LiquidBackground: React.FC = () => (
    <div className="ambient-background" />
);

export default LiquidBackground;
